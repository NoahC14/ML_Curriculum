# Graph Learning Notebooks

Module 13 notebooks favor minimal implementations over framework-heavy abstractions.

- `GL-01-message-passing.ipynb` studies aggregation, permutation equivariance, and Laplacian smoothing on small graphs.
- `GL-02-node-classification.ipynb` trains a small GCN-style classifier on the Karate Club graph.
